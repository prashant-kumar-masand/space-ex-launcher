import { SuccessfulLandingPipe } from './successful-landing.pipe';

describe('SuccessfulLandingPipe', () => {
  it('create an instance', () => {
    const pipe = new SuccessfulLandingPipe();
    expect(pipe).toBeTruthy();
  });
});
